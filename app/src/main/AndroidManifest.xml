<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeeteshgupta.gymkhanainventory">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Gsec" />
        <activity android:name=".RegisterStudent" />
        <activity android:name=".CoordinatorActivity" />
        <activity android:name=".changepassCoordintaor" />
        <activity
            android:name=".AddInventory"
            android:label="@string/title_activity_add_inventory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".addItem" />
        <activity android:name=".EditInventory" />
        <activity android:name=".Scan" />
        <activity android:name=".GiveInventory" />
        <activity android:name=".CoordinatorRecord" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".StudentInventory" />
        <activity android:name=".GymkhanaInventory"></activity>
    </application>

</manifest>